'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
var runServer = exports.runServer = function runServer(server, config) {

    if (config.port) {
        server.listen(config.port, config.host, function (err) {
            if (err) {
                console.error(err);
            }
            console.info('==> 💻  Open ' + config.protocol + '://' + config.host + ':' + config.port + ' in a browser to view the app.');
        });
    } else {
        console.error('==>     ERROR: No PORT variable has been specified');
    }
};