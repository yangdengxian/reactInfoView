"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

var createServer = exports.createServer = function createServer(serverElement) {
    var parent = arguments.length <= 1 || arguments[1] === undefined ? {} : arguments[1];


    if (typeof serverElement.type.buildServer === 'function') {
        var _ret = function () {
            var serverComponents = serverElement.type.buildServer(serverElement.props, parent);

            var nextParent = Object.assign({}, parent, serverComponents, { children: false } // It is not necessary to carry child React Elements here.
            );

            if (typeof serverComponents.children !== "undefined") {

                var children = serverComponents.children;

                if (serverComponents.children instanceof Array === false) {
                    children = [serverComponents.children];
                }

                serverComponents.children = children.map(function (el) {
                    return createServer(el, nextParent);
                });
            }

            return {
                v: serverComponents
            };
        }();

        if ((typeof _ret === "undefined" ? "undefined" : _typeof(_ret)) === "object") return _ret.v;
    }

    return false;
};